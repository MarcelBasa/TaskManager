#include "TaskManager.h"

int main()
{
    TaskManager taskManager;

    //taskManager.addTask();
    taskManager.addTask();

    //taskManager.loadFromFile("data.txt");
    taskManager.showTasks();

    return 0;
}